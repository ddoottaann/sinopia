- name: test raw module
 
  tasks:
    - name: run ipconfig
      win_command: ipconfig
      register: ipconfig
    - debug: var=ipconfig
    
- name: Create temporary build directory
  win_tempfile:
    state: directory
    suffix: build
